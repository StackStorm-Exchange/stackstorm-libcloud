---
name: enable_cdn_for_container
runner_type: python-script
description: Enable CDN for container and return the CDN URL
enabled: true
entry_point: enable_cdn_for_container.py
parameters:
  credentials:
    type: string
    description: Name of the credentials set (as defined in the config) to use.
    required: true
  container_name:
    type: string
    description: Name of the container to enable the CDN for.
    required: true
  extra_kwargs:
    type: object
    description: Additional keyword arguments which will be passed to the underlying Libcloud method.
    required: false
